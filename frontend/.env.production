# Help content is bundled from src/content/ — no backend URL needed.
# Used by the app but not defined here (set them if those features are needed):
#   VITE_APPWRITE_FUNCTION_AFFILIATE_REDIRECT_DOMAIN
#   VITE_TURNSTILE_SITE_KEY
